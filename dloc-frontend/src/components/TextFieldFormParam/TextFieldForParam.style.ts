import { TextFieldProps } from '@mui/material';

const TextFieldForParamStyle = {
  TextField: {
    autoFocus: true,
    fullWidth: true,
    margin: 'dense',
    required: true,
    type: 'text',
    variant: 'standard',
  } as TextFieldProps,
};

export default TextFieldForParamStyle;
