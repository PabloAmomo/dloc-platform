import { SxProps } from '@mui/material';

const ConfigInstructionsLineStyle = {
  ConfigLineProps: {
    sx: { display: 'flex', flexDirection: 'column' } as SxProps,
  },
};

export default ConfigInstructionsLineStyle;
